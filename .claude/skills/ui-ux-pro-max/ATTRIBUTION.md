# Attribution

Vendored from [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill)
(the `ui-ux-pro-max` sub-skill only), MIT licensed — see `LICENSE.txt` in this
folder. `${CLAUDE_PLUGIN_ROOT}` path references in the upstream `SKILL.md`
were rewritten to repo-root-relative paths (`.claude/skills/ui-ux-pro-max/...`)
since this is vendored directly rather than installed as a marketplace plugin.

Pull updates by re-copying `.claude/skills/ui-ux-pro-max/` from the upstream
repo's `.claude/skills/ui-ux-pro-max/` and re-applying the same path rewrite.
